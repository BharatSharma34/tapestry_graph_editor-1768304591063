name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sync
        run: |
          curl -X POST "https://voila-api.mmopro.in/sync-app" \
            -H "Content-Type: application/json" \
            -d '{"app": "tapestry_graph_editor-1768304591063", "repo": "https://github.com/BharatSharma34/tapestry_graph_editor-1768304591063.git", "github_token": "${{ secrets.PRIVATE_GITHUB_TOKEN }}"}'